# NY-V1 Network Discovery — Codex HOLD Remediation Report

- **TASK_ID:** `NY-V1-NETWORK-DISCOVERY-001-CODEX-HOLD-REMEDIATION-01`
- **Starting SHA:** `9ff8e3a4d02b5c0bc0bb47bb1268154438e74854`
- **Ending SHA (remediation code/tests):** `6e7ff104c850c7616e300ca45ac7f60923c0598d`
- **Branch:** `kimi/NY-V1-NETWORK-DISCOVERY-001-CONTINUE`
- **Review Report:** `C:\projects\NetYemen-codex-review\docs\reports\NY-V1-NETWORK-DISCOVERY-001-CODEX-REVIEW.md`

## Finding-by-Finding Remediation Matrix

| ID | Severity | Finding | Remediation | Affected Files | Tests Added |
|---|---|---|---|---|---|
| F-01 | HIGH | Client idempotency keys are not UUIDs | Replaced the base-36 timestamp token with a standards-compliant UUID v4 generated by `UuidGenerator`. The existing `pendingIdempotencyKeyProvider` preserves the same key across retries and clears it only on success. | `lib/core/utils/uuid_generator.dart`<br>`lib/features/network_requests/presentation/network_request_providers.dart`<br>`lib/features/network_requests/data/fake_network_request_repository.dart` | `test/core/utils/uuid_generator_test.dart`<br>`test/features/network_requests/submit_request_notifier_test.dart` |
| F-02 | HIGH | Fresh users cannot authenticate in the new app shell | Surfaced the existing Supabase phone-OTP sign-in flow from the new `AppShell`. Added `AuthRequiredGate` to authenticate `MyRequestsScreen` and `AddRequestScreen`. `ProfileScreen` now shows sign-in/sign-out based on `currentUserProvider`. `OTPScreen` returns to `AppShell` on success. `currentUserProvider` watches `authStateProvider`, so the UI rebuilds automatically and existing Supabase sessions are restored on startup. | `lib/providers/app_providers.dart`<br>`lib/features/auth/presentation/auth_required_gate.dart`<br>`lib/features/network_requests/presentation/add_request_screen.dart`<br>`lib/features/network_requests/presentation/my_requests_screen.dart`<br>`lib/features/profile/presentation/profile_screen.dart`<br>`lib/screens/auth/otp_screen.dart` | `test/features/auth/auth_gate_test.dart`<br>`test/features/profile/profile_screen_test.dart`<br>`test/features/network_requests/my_requests_notifier_test.dart` |
| F-03 | MEDIUM | Idempotent replay is not atomic | Rewrote `submit_network_addition_request` to perform an atomic `INSERT ... ON CONFLICT (requester_user_id, idempotency_key) DO NOTHING RETURNING id`, falling back to a SELECT only on conflict. This removes the SELECT-then-INSERT race. | `supabase/migrations/20260727130000_netyemen_network_addition_requests.sql` | `supabase/tests/005_network_discovery_and_requests.sql` (POS-02 replay still exercised) |
| F-04 | MEDIUM | Resolution lifecycle is not a defined state machine | Added explicit source/destination transition validation in `resolve_network_addition_request`. Terminal statuses (`approved`, `rejected`, `matched_existing`, `cancelled`) cannot be rewritten, and terminal-to-`under_review` reopening is blocked. | `supabase/migrations/20260727130000_netyemen_network_addition_requests.sql` | `supabase/tests/005_network_discovery_and_requests.sql` (NEG-12) |
| F-05 | MEDIUM | Dart and database SSID normalization contracts differ | Updated `ScanMatcher.normalizeForMatching` to mirror `public.normalize_ssid`: trim, Unicode NFC normalization via `unorm_dart`, lowercase, collapse whitespace to hyphens, collapse hyphens, trim surrounding hyphens. The network search query is now normalized the same way. | `lib/features/network_discovery/data/scan_matcher.dart`<br>`lib/features/network_discovery/presentation/network_discovery_providers.dart`<br>`pubspec.yaml` / `pubspec.lock` | `test/features/network_discovery/scan_matcher_test.dart` |
| F-06 | LOW | Production integration and adverse scan paths lack Dart tests | Added focused Dart tests for UUID validity/idempotency and auth-gate behavior. | `test/core/utils/uuid_generator_test.dart`<br>`test/features/network_requests/submit_request_notifier_test.dart`<br>`test/features/auth/auth_gate_test.dart`<br>`test/features/profile/profile_screen_test.dart`<br>`test/features/network_requests/my_requests_notifier_test.dart`<br>`test/features/network_discovery/scan_matcher_test.dart` | same |
| F-07 | LOW | Request cancellation failures are silently swallowed | Cancellation errors in `MyRequestsScreen` are now surfaced with an Arabic RTL `SnackBar`. | `lib/features/network_requests/presentation/my_requests_screen.dart` | existing repository tests continue to cover failure paths |

## Auth Governance Note

- **OD-AUTH-01** (production SMS OTP gateway) remains unapproved.
- No new SMS provider was selected or integrated.
- No remote Supabase Auth configuration was changed.
- The remediation uses only the existing repository's Supabase phone-OTP sign-in screens (`lib/screens/auth/login_screen.dart` and `otp_screen.dart`).

## Validation Results

| Check | Command | Result |
|---|---|---|
| Flutter dependencies | `flutter pub get` | PASS |
| Static analysis | `flutter analyze` | PASS — 0 issues |
| Unit/widget tests | `flutter test` | PASS — 39 tests |
| Debug APK build | `flutter build apk --debug` | PASS — `build/app/outputs/flutter-apk/app-debug.apk` |
| JDK used | `flutter doctor -v` | Microsoft OpenJDK Runtime Environment 17.0.20+8-LTS |
| Local Supabase start | `npx supabase start` | PASS (loopback stack) |
| Database reset | `npx supabase db reset --no-seed` | PASS |
| SQL 001 | `docker exec ... psql ... -v ON_ERROR_STOP=1 < supabase/tests/001_core_schema_contract.sql` | PASS |
| SQL 002 | `docker exec ... psql ... -v ON_ERROR_STOP=1 < supabase/tests/002_core_authorization_positive.sql` | PASS |
| SQL 003 | `docker exec ... psql ... -v ON_ERROR_STOP=1 < supabase/tests/003_core_authorization_negative.sql` | PASS |
| SQL 004 | `docker exec ... psql ... -v ON_ERROR_STOP=1 < supabase/tests/004_core_invariants.sql` | PASS |
| SQL 005 | `docker exec ... psql ... -v ON_ERROR_STOP=1 < supabase/tests/005_network_discovery_and_requests.sql` | PASS |
| Core foundation verifier | `powershell -ExecutionPolicy Bypass -File scripts/verify_netyemen_core_foundation.ps1` | PASS |
| Whitespace check | `git diff --check` | PASS (only LF/CRLF conversion warnings) |
| Secret scan | manual grep for `sb_secret`, `service_role`, `private_key`, `BEGIN` in diff | PASS — no new committed secret or service-role key |

## Commits Created

1. `001ebe0` — `fix(app): use valid UUID request idempotency keys`
2. `2d80ab2` — `fix(db): atomic idempotent submit and coherent resolution state machine`
3. `1ba8ec1` — `feat(auth): add reachable NetYemen authentication flow`
4. `91ebc8b` — `fix(app): align Dart SSID normalization with PostgreSQL contract`
5. `6e7ff10` — `test(app): verify Codex HOLD remediation`
6. `<docs-sha>` — `docs(report): record Codex HOLD remediation`

## Push / PR

- **Branch pushed:** `kimi/NY-V1-NETWORK-DISCOVERY-001-CONTINUE`
- **Draft PR #5:** updated via branch push, kept Draft and unmerged.

## Security / Privacy Preservation

- Wi-Fi scanning remains explicitly user-triggered only (button presses).
- Only SSID strings are handled; BSSID, MAC, location, signal strength, frequency, and passwords are neither persisted nor transmitted.
- `supabase/.temp`, `supabase/.branches`, build outputs, local keys, and credentials were not committed.

## Remaining Blockers

- None for the Codex HOLD findings. The only outstanding governance item is **OD-AUTH-01** (production SMS OTP gateway provider selection), which was explicitly out of scope and requires a separate product decision.

## Outcome

**PASS**

All BLOCKER, HIGH, and MEDIUM findings from the Codex review have been remediated and independently re-tested. LOW findings F-06 and F-07 were also resolved because they were directly related and safe. The local Supabase authorization foundation remains intact, cross-user isolation is enforced, and the production request path now uses a valid UUID contract behind a reachable authentication flow.
