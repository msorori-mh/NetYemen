/// Stable product labels used by both the customer and administration apps.
///
/// Runtime credentials and service bindings belong in [AppConfig], not here.
class AppConstants {
  static const String appName = 'WASEL NET';
  static const String appNameAr = 'واصل نت';
  static const String appVersion = '1.0.0';
}
