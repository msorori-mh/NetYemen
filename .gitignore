.dart_tool/
.packages
.pub/
.pub-cache/
build/
.flutter-plugins
.flutter-plugins-dependencies
.idea/
.vscode/
*.iml
android/.gradle/
android/local.properties
android/key.properties
*.jks
*.keystore
.env
.env.*

# Local Supabase runtime state
supabase/.temp/
supabase/.branches/

# Android build outputs
android/.kotlin/
android/.gradle/

# Hive orchestration workspace (separate git repo) + roster state
hive/
roster.json
roster-backups/

# Build deliverables (signed APK/AAB) — never commit binaries
dist/

# Admin dashboard runtime config (holds project URL + publishable key)
admin/config.js

# Vercel local project links
.vercel/

# Local schema dumps
schema_ssid.sql
